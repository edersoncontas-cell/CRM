# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# database (SQLite local dev)
*.db
*.db-journal
prisma/dev.db
prisma/dev.db-journal

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env*.local

# typescript
*.tsbuildinfo

# claude code — preferências e permissões locais desta máquina.
# Nunca versionar: guarda liberação de comando (inclusive o push de produção),
# que é decisão de quem está na máquina, não regra do projeto.
.claude/settings.local.json
